<% fav = Favorite.where(user_id: current_user.id, item_id: item.id).first %>
<%= link_to '<span class="glyphicon glyphicon-star"></span>'.html_safe, user_favorite_path(user_id: current_user.id, id: fav.id), method: :delete, remote: true, class: "btn btn-xs btn-warning", title: "Remove favorite"%>
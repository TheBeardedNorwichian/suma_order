$('#fav_but_<%= @new_fav.item.id %>').html(
  "<%= escape_javascript(render 'favorites/delete', item: @new_fav.item) %>"
  );
$("#item_row_<%= @new_fav.item.id %>").attr('class', 'warning');
<%= add_gritter("#{@new_fav.item.description} added to your favorites", title: "Favorite Added") %>
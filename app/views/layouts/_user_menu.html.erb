<li id="fat-menu" class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
    <%= @current_user.name %> <b class="caret"></b>
  </a>

  <ul class="dropdown-menu">
    <li class="nav-header">My Stuff</li>
    <li class="dropdown-submenu">
      <%=link_to "My Order", user_order_orderitems_path(user_id: current_user, order_id: current_order) %>
      <ul class="dropdown-menu">
        <% if current_user_orderitems.empty? %>
          <div class="alert alert-warning"><li><%= "Your order is empty" %></li></div>
        <% elsif %>
          <%current_user_orderitems.each do |oi| %>
          <li><%= oi.item.description %>
            <span class="badge pull-right"><%= oi.quantity %></span>
          </li>
          <% end %>
        <% end %>
        <li><a tabindex="-1" href="#"><strong>Total: <%= number_to_currency(total_price(current_user_orderitems), unit: "£") %></strong></a></li>
      </ul>
    </li>
    <li><%=link_to "Profile", '#' %></li>
    <li><%=link_to "Favorites", user_favorites_path(user_id: current_user) %></li>
    <li><%=link_to "Order History", '#' %></li>
    <% if admin? %>
    <li class="divider"></li>
    <li class="nav-header">Admin</li>
    <li><%=link_to "Users", users_path %></li>
    <li><%=link_to "Groups", groups_path %></li>
    <li><%=link_to "New Group", new_group_path %></li>
    <% end %>
    <li class="divider"></li>
    <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
  </ul>
</li>